# barcode-reader

#barcode-nosql-db
- Requirements:
  - node.js & npm `https://nodejs.org/en/`
  - after install nodejs, `cd barcode-nosql-db`
  - `npm install`
  - `npm install -g nodemon`
  - `nodemon`